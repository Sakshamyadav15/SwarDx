services:
  # Backend API Service
  - type: web
    name: swardx-backend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: ./
    plan: standard
    healthCheckPath: /docs
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
